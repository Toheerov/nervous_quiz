# 🧠 Черепные нервы — Тренажёр

Викторина и карточки для изучения 12 пар черепных нервов.

---

## 📱 Как собрать APK через GitHub (без Android Studio)

### Шаг 1 — Создай репозиторий на GitHub

1. Зайди на [github.com](https://github.com) → нажми **"New repository"**
2. Название: `cranial-nerves-quiz`
3. Поставь **Public**
4. Нажми **"Create repository"**

---

### Шаг 2 — Загрузи файлы

**Вариант А — через браузер (без Git):**

1. В созданном репозитории нажми **"uploading an existing file"**
2. Перетащи ВСЕ файлы из папки `cranial-nerves-app`
   ⚠️ Важно: папку `.github` тоже нужно загрузить (она скрытая — покажи скрытые файлы)
3. Нажми **"Commit changes"**

**Вариант Б — через Git (если установлен):**
```bash
cd cranial-nerves-app
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ТВО_ИМЯ/cranial-nerves-quiz.git
git push -u origin main
```

---

### Шаг 3 — Дождись сборки APK

1. Зайди в репозиторий → вкладка **"Actions"**
2. Увидишь процесс **"Build Android APK"** — он запустится автоматически
3. Подожди **5–10 минут** (индикатор крутится)
4. Когда появится зелёная галочка ✅ — готово!

---

### Шаг 4 — Скачай APK

1. Нажми на завершённый workflow
2. Внизу страницы найди раздел **"Artifacts"**
3. Скачай **`cranial-nerves-quiz-apk`** (zip-архив)
4. Распакуй — внутри файл `app-debug.apk`

---

### Шаг 5 — Установи на Android

1. Перекинь `app-debug.apk` на телефон (через USB, Telegram себе, Google Drive)
2. Открой файл на телефоне
3. Если появится предупреждение — нажми **"Установить всё равно"**
   (это нормально для приложений не из Play Store)
4. Готово! 🎉

---

## 🛠 Локальная разработка

```bash
npm install
npm run dev
```

---

## 📦 Структура проекта

```
cranial-nerves-app/
├── src/
│   ├── main.jsx          # Точка входа
│   └── App.jsx           # Весь код приложения
├── .github/
│   └── workflows/
│       └── build.yml     # Автосборка APK
├── index.html
├── vite.config.js
├── capacitor.config.json
└── package.json
```
